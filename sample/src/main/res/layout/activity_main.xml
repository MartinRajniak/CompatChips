<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="sk.rajniak.chips.sample.MainActivity">

    <sk.rajniak.chips.RecipientEditTextView
        android:id="@+id/recipient_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint"
        android:minHeight="58dp" />

    <sk.rajniak.chips.RecipientEditTextView
        android:id="@+id/focus_tv"
        android:layout_below="@+id/recipient_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="58dp" />

    <Button
        android:id="@+id/append"
        android:layout_below="@+id/focus_tv"
        android:layout_width="match_parent"
        android:layout_height="58dp"
        android:layout_marginTop="16dp"
        android:text="@string/append"
        />

</RelativeLayout>
